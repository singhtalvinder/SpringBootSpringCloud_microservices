spring.application.name=limits-service

#Pickup config from spring-cloud-config-server-project and 
# this filename needs to be changed from application.properties to something else.

spring.cloud.config.uri=http://localhost:8888
#set which profile you want to be active out of the many profiles(here: dev, qa, default).
spring.profiles.active=dev

#limits-service.minimum=99
#limits-service.maximum=9999
